"""
이진변환 : 1의 갯수를 이진법으로 표현한 문자열.
s가 1이 될때까지 이진변환을 한다.
반환 : 이진변환을 시도한 횟수, 변환과정에서 제거된 0의 갯수

🔹 bin() : 문자열 이진수로 변환.
- 반환값 : "ob" + "변환된 이진수"
🔹iterable.count("찾을 요소")
"""
s = "110010101001"
transform_cnt = 0
zero_cnt = 0

while s != "1":
    transform_cnt += 1
    zero_cnt += s.count("0")

    #이진변환
    s = bin(s.count("1"))[2:]

print([transform_cnt, zero_cnt])

print("")

# (2) '숫자 배열'에서 특정 숫자 갯수 찾기
s = [1,2,3,2,3,5]
print(f"(2) {s.count(2)}")

