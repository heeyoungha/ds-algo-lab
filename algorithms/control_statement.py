text = """
=========================================
제어문 (Control Statements)

조건문과 반복문을 활용한 제어 흐름 학습

🔹 range 객체
🔹 삼항연산자
🔹 조건문 (if/else)
🔹 반복문 (while)
"""
print(text)

text = """
=========================================
🔹 range 객체 : 숫자 시퀀스를 생성하는 내장 자료형

특징:
- 인덱스 접근 가능
- iterable: 반복 가능한 객체
- immutable: 값을 바꿀 수 없음
- 메모리 효율: 숫자를 미리 저장하지 않고 필요할 때 계산
"""
print(text)

print("")

print("(1) 기본 range 생성")

print(f"range(5) => {range(5)}")
print(f"type => {type(range(5))}")
print(f"range(5)[2] => {range(5)[2]}")

print("\n(2) 시작, 끝, 간격 지정")
print(f"range(2, 10, 2) => {range(2, 10, 2)}")
print(f"list(range(2, 10, 2)) => {list(range(2, 10, 2))}")

print("\n(3) 역순 range")
print(f"range(10, 2, -2) => {range(10, 2, -2)}")
print(f"list(range(10, 2, -2)) => {list(range(10, 2, -2))}")

text = """
=========================================
🔹 삼항연산자
"""
print(text)

"""
문제: 배수이면 1 아니면 0

루틴:
1. num % n == 0 조건 확인
2. 참이면 1, 거짓이면 0 반환

"""

num = 38
n = 2
print(f"(1) {1 if (num % n == 0) else 0}")

"""
문제: flag에 따라 다른 연산 수행

루틴:
1. flag가 True이면 a + b
2. flag가 False이면 a - b
"""

a, b = 2, 1
flag = False

print(f"(2) {a + b if flag else a - b}")


text = """
=========================================
🔹 공배수 판별
"""
print(text)

"""
문제: 두 조건을 모두 만족하면 1 아니면 0

루틴:
1. number % n == 0 확인
2. number % m == 0 확인
3. 두 조건 모두 참이면 1, 아니면 0

예시:
number = 60, n = 2, m = 3
정답: 1
"""

number = 60
n = 2
m = 3

# 방법 1: bool과 & 연산자 사용
result1 = int(bool(number % n == 0) & bool(number % m == 0))
print(f"방법 1: {result1}")

# 방법 2: 논리 연산자 활용
result2 = int(not((number % n) or (number % m)))
print(f"방법 2: {result2}")

text = """
=========================================
🔹 조건에 따라 요소 다르게 계산하기
"""
print(text)

"""
문제: n이 짝수면 짝수 제곱의 합, 홀수면 홀수의 합

루틴:
1. n % 2 == 0 확인 (짝수 여부)
2. 짝수면: range(2, n+1, 2)의 각 요소 제곱의 합
3. 홀수면: range(1, n+1, 2)의 각 요소의 합

예시:
n = 4 (짝수)
정답: 2² + 4² = 4 + 16 = 20
"""

n = 4

# 방법 1: 기본 for문 사용
answer = 0
if (n % 2 == 0):
    for i in range(2, n+1, 2):
        answer = answer + i*i
else:
    for i in range(1, n+1, 2):
        answer = answer + i
print(f"방법 1 (n={n}) => {answer}")

# 방법 2: sum과 generator expression 사용
if (n % 2):  # 홀수
    answer = sum(range(1, n+1, 2))
else:
    answer = sum(i*i for i in range(2, n+1, 2))
print(f"방법 2 (n={n}) => {answer}")


text = """
=========================================
🔹 while 반복문 : 조건식이 참인 동안 계속 수행

루틴:
1. while [조건식] 형태로 사용
2. 조건식이 True인 동안 반복
3. 조건식이 False가 되면 종료
"""
print(text)


print("(1) while을 사용한 문자열 패딩")
num = 35
num_str = str(num)
while len(num_str) < 4:
    num_str += "0"
print(f"while 사용 => {num_str}")

print("\n(2) zfill() 메서드 사용 (간단한 방법)")
num_str2 = str(num).zfill(4)
print(f"zfill() 사용 => {num_str2}")
