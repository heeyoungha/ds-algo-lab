text = """
=========================================
2개의 행렬 arr1과 arr2를 입력받아, 행렬 덧셈의 결과를 반환하는 함수

🔹 zip() 
- 동일한 인덱스 위치의 요소끼리 묶어서 (a, b, c, ...) 형태의 튜플을 만든다.
- 전달된 iterable 중 가장 짧은 길이에 맞춰 멈춘다.
=========================================
"""
print(text)

A = [[1, 2], [2, 3], [4, 5], [6, 7]]
B = [[4, 5], [6, 7], [8, 9], [10, 11]]

# (1) 리스트 컴프리헨션 + zip()
for a, b in zip(A, B):
    print(f"{a}, {b}")

print([[c + d for c, d in zip(a, b)] for a, b in zip(A, B)])

# (2) 새로운 배열에 담기
answer = []
for i in range(len(A)):
    row = []
    for j in range(len(A[0])):
        row.append(A[i][j] + B[i][j])
    answer.append(row)
print(answer)

# (3) A배열과 B배열을 합치기
for i in range(len(A)):
    for j in range(len(A[0])):
        A[i][j] = A[i][j] + B[i][j]
print(A)

text = """
=========================================
🧩 연습문제 : 직사각형 만들기 

각 요소에서 x, y 좌표를 분리하고
각 좌표에서 한 번만 나타나는 값을 찾기

🔹 리스트 컴프리헨션으로 좌표 분리
🔹 count() 메서드로 중복 요소 찾기
"""
print(text)


v = [[1, 4], [3, 4], [3, 10]]

# x좌표와 y좌표를 각각 분리
xs = [x for x, y in v]
ys = [y for x, y in v]

# 각 좌표에서 한 번만 나타나는 값 찾기
x = [num for num in xs if xs.count(num) == 1][0]
y = [num for num in ys if ys.count(num) == 1][0]

print(f"중복되지 않는 좌표 => [{x}, {y}]")
